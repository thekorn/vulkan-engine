name: build test
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
concurrency:
  # Cancels pending runs when a PR gets updated.
  group: ${{ github.head_ref || github.run_id }}-${{ github.actor }}
  cancel-in-progress: true
jobs:
  build-test:
    strategy:
      fail-fast: false
      matrix:
        # os: [macos-latest, ubuntu-latest]
        os: [ubuntu-latest]
    runs-on: ${{matrix.os}}
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      # Install Nix and use that to run our tests so our environment matches exactly.
      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@main
      # Run our checks to catch quick issues
      - run: nix flake check

      - name: spellcheck
        run: nix develop -c codebook-lsp lint --unique -s .

      - name: lint (zlinter)
        run: nix develop -c zig build lint

      - name: build & test
        run: nix develop -c zig build test --summary all

      - name: coverage (zcov)
        run: nix develop -c zig-cov test --format=html --output=coverage.html -- --summary all

      - name: upload coverage report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: coverage-report
          path: coverage.html
          if-no-files-found: ignore

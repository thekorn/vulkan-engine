zig-out
.zig-cache
# macOS Finder metadata
.DS_Store
# Zig fetches third-party packages into a local `zig-pkg/`
# directory next to `build.zig` (in addition to the global cache).
zig-pkg
# Self-contained report generated by `zig-cov test --format=html`.
coverage.html
# Dear ImGui writes per-user UI state (window positions, sizes, …)
# to `imgui.ini` next to the binary at runtime.
imgui.ini
# nix reuslt
result
# Amp's generated orb portal metadata
.amp/portals/*
